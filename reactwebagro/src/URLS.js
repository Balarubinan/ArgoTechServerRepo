// plop down all the required urls in here
const devUrl="http://localhost:5000"
const prodUrl="https://argo-server-1.herokuapp.com"
// change to dev before dev and change to prod before push
module.exports={
    baseUrl:devUrl
}